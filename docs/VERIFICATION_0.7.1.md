# 0.7.1 验证记录

验证日期：2026-09-07。

- JavaScript 自动测试：220 项通过；新增非标准键盘事件兼容性回归检查。
- Python 自动测试：98 项通过；Windows `CODEX_HOME` 路径在 Linux CI 模拟环境中保持 Windows 分隔符。
- 扩展脚本语法检查：55 个文件通过；JSON 清单检查：3 个文件通过。
- 浏览器扩展核心清单、悬浮入口、专业翻译、字幕存档和知识导出测试通过。

真实网页仍可能受浏览器特殊页面、站点 CSP、跨域 iframe、DRM 和权限设置限制；这些限制不应导致内容脚本整体崩溃。
