# 网页扩展修复说明 · 2026-09-04

本次保留网页扩展，不包含桌面端。桌面端临时源码、启动脚本、测试和 PyAudioWPatch 专用依赖已撤回；未修改学习资料，未发布到 GitHub。

## 已修复

### 双语 MD / PDF、播放入口与登录失败提示（本次更新）

- 移除 TXT 导出入口，视频双语学习文本改为 Markdown 和 PDF。MD 保留原文、译文及开始/结束时间戳；PDF 打开独立排版预览，点击“打印 / 保存 PDF”并选择“另存为 PDF”。该导出不调用模型、不需要额外 PDF 服务；未译完的记录不伪装成完整双语文件。
- 预览页用纯文本渲染原文/译文，避免文档中的 HTML 执行；支持中文、长句换行、A4 分页。预览快照转入该预览标签页的临时存储，刷新仍可阅读，不修改学习记录。
- 进度明确区分“已提取原文 N 条”和“译文 X/N 条”；0/1043 表示读到 1043 条原文，但尚无一条成功译文，不表示已经翻译完成。另显示已就绪时间范围及当前播放片段是否已译到。
- “暂停提前翻译”只暂停后续准备，不关闭已有译文字幕；“显示译文字幕”重新挂载已有字幕，不启动新的模型翻译。实际播放仍点击视频播放器自己的 ▶。需要停止全部任务时用“结束翻译 / 字幕会话”。
- 在启动/继续专业批次前核查所选引擎的登录状态。实测本地 `/codex/status` 返回 `loggedIn:false`、`detail:Not logged in`，日志有 Codex 尚未登录错误：这是此次有原文无译文的明确阻断因素。不会自动改成 API Key 或其他引擎。
- 登录需要用户本人操作，参考 [OpenAI Docs 认证说明](https://learn.chatgpt.com/docs/auth)。完成后重载扩展、刷新视频页，再开始/继续提前翻译。实际登录后全流程仍待验证。

### 提前翻译等待与断点恢复（最新修正）

- 专业字幕不再用一个长 HTTP 请求等待整批模型结果。改为本地后台任务：短请求提交/查询，返回正在运行、成功或失败。相同任务编号重复查询不会再次启动模型。
- 翻译队列不再依赖播放器时钟推进；增加独立定时推进，以及浏览器后台休眠后的定时唤醒和本机断点恢复。视频暂停或菜单关闭，不要求先播放视频才能继续准备。
- 菜单/右栏显示本批字幕条数、所选引擎和已等待秒数。服务未启动、旧服务缺少新接口、模型失败、未知响应及超过 10 分钟未完成均明确提示，不再无限显示同一进度。状态文字仍不进入视频字幕框。
- 停止会停止派发后续批次；已经提交到模型的那一批可能继续完成。再次处理同一视频、同一设置时保留待处理编号，复用已完成译文，避免因重试请求重复调用模型。不会自动退回 NLLB。
- 本地任务结果仅缓存在内存，完成后保留最多一小时；重启本地服务会清除此缓存。已回填的学习记录在扩展存储中保留。浏览器完全退出或系统休眠期间不承诺持续推进。
- 此轮实际检查：原默认本地服务地址连接被拒绝；现已启动更新后的服务，并验证健康接口和提前字幕任务接口存在。尚未通过用户登录的 CQF 页面调用真实模型验证。

设计依据：[Chrome 扩展后台生命周期说明](https://developer.chrome.com/docs/extensions/develop/concepts/service-workers/lifecycle)指出，长时间无活动或等待 fetch 响应可能终止后台，因此任务状态需要持久化，不能仅依赖内存中的队列。

### 专业术语库

- 扩充为 332 个词条、78 个别名，覆盖金融、量化、经济、数学、统计、编程和通用学术；未恢复电池材料词条。
- 按当前原文和上下文检索相关词条，而非固定截取词库前几十项。支持缩写、别名、重音及连字符变体，保留专业领域与易混淆说明。
- 网页/PDF 设置支持编辑语境说明与别名，JSON/CSV 导入导出保留这些信息；迁移保留用户已有自定义译法。视频后端采用同源预置库，但网页自定义词条暂不自动同步到视频后端。
- 详见 [专业术语库说明](专业术语库说明.md)。

### 提前精译、CQF 字幕与学习保存（本轮新增）

- 菜单分成“提前全文翻译”和“开始实时翻译”两个独立按钮。提前按钮自动检测当前视频；找不到完整来源时明确报错，不悄悄改为实时收音。实时按钮随播放采集声音。
- CQF / Brightcove 优先当前播放器的英文 captions/subtitles；兼容 HTTPS sources、Video.js 轨道及延迟加载。排除缩略图 metadata，不遍历课程播放列表，不获取密钥或解密 DRM。
- 原始字幕每条单独保留编号、开始和结束时间。Codex / Kimi 一次读取一组完整语义内容及前后原文，按编号返回每条译文。缺条、空译文、重复/错误编号或丢失受保护符号会拒绝整批结果；后端失败时显示原因，停止后重开可补译。前端回填校验最多尝试三次；不会平均切割译文或移动时间戳。
- 播放字幕依据播放器 currentTime，不使用独立累积时钟。监听暂停、拖动、倍速事件，补充定时校准；保留字幕空档和重叠，切换媒体时清空旧字幕并暂停。新课程页仍需点击启动；同页换视频目前需停止后重新开始，避免误用上一段缓存或无意消耗额度。
- 菜单和右栏显示读取中、音轨识别中、已译 X/Y 条、前段就绪至何处、全部完成及失败提示。可以边译边播，但未译区间仍需等待；想避免等待，应在全部完成后播放。不会自动控制播放/暂停，字幕画面不显示进度状态字。
- “保存视频双语学习文本”现提供 MD 和 PDF：包含标题、来源、时间戳及逐条原文/译文。SRT 保留精确结束时间。记录仍有未译条目时阻止完整导出，避免误当全文；原文和成功译文已保存在本机，可恢复补译。
- 完整音轨支持可直接读取的 HTTPS 普通 MP3/M4A/MP4/WAV/FLAC/OGG/WebM 文件；上限 128 MB、4 小时。当前不下载 HLS/DASH 清单分片，不解密受保护媒体。音频在内存中处理，不新增录音文件；ASR 精度并不等于逐字无误。
- PDF 阅读器显示名称统一为 Quant Scholar PDF Reader；“Codex / Kimi 精译 PDF”直接提交原文段落。PDF 专业入口仅接受设置里的 Codex 或 Kimi 套餐引擎，不经过 NLLB，也不自动切换其他服务商。PDF 原译文对照、缓存和笔记功能保留。
- 旧版合并段落字幕缓存不用于新时轴；重新提取原始字幕再翻译，旧记录在会话归档中保留。

以上针对 Chrome / Edge 网页扩展。本轮没有宣称 Safari/iOS、跨域播放器全屏或所有网站均已实测。

- 本轮“全文”的含义：从开始采集起，实际听到并识别出的内容逐段翻译、连续保存，不做摘要或节选；不等于提前读取整段视频，也不能保证语音模型逐字零误识别。
- 音频完整性：连接未就绪时不再主动丢弃待发送块；不因识别滞后丢块；不对互不重叠音频块做文本去重，保留真实重复语句。
- 原文先存：完整句子提交后先写入本机，终稿按唯一记录编号回填；失败原文保留，停止后可点击“补译此段”。后台专业翻译自动尝试最多三次。
- 停止收尾：停止收音后提交末尾不足三秒音频，等待队尾处理；超时或连接失败会提示记录可能不完整，不能当作全部成功。
- 字幕只保留正文，隐藏引擎、等待及错误状态文案；提供紧凑译文、紧凑双语、仅右侧栏三种显示方式；字幕框不拦截鼠标操作，长段全文保留在侧栏。
- 普通网页双语记录增加“存为笔记”，直接保存原文、译文、网页和时间戳；笔记不依赖模型润色成功，不再超过 100 条就自动删旧；连续保存串行写入，避免互相覆盖。旧版本已删掉的笔记无法由此恢复。
- 笔记保存在当前浏览器扩展的本机存储，关闭侧栏或重启后可恢复；不自动跨设备或跨浏览器同步。卸载扩展或清理扩展数据可能丢失，请先复制或备份重要笔记。

- 实时字幕：主页面消息明确确认接收；失败时重试挂载，并在菜单显示错误，不再静默丢失。
- 页面重绘：字幕卡被移除时重新挂回，保留文字和字号；主动停止后不会自行恢复。
- 恢复隔离：仅采集所在标签页恢复字幕；丢弃停止后及旧连接返回的结果。
- 网页原字幕：无关页面动画不再反复重置字幕检测计时，避免一直等不到字幕。
- 音频识别：分块由 1 秒调整为 3 秒；保留原语言识别，再由所选引擎翻译，不让 Whisper 默认先转英语。识别质量、延迟仍取决于声音和硬件。
- 双语侧栏：直接显示同一记录的原文和译文，避免按时间重新分组错配；默认双语实际生效；切换显示不再重复调用翻译模型；复制与导出遵循显示模式。
- 记录时间：优先使用收到原文时的播放器进度，而非较晚的译文返回时间。仍可能有识别延迟，跨域内嵌播放器的进度不保证可读。
- 清空记录：实时运行时清空已保存记录，不再破坏会话导致后续终稿无法保存。
- 设置：字幕字号、位置实时应用；语言、引擎、模型等在下次开始时生效并有提示。
- 菜单入口：PDF 和学习工作台绑定菜单所属网页；打开失败明确显示原因；工作台打开不等待设置请求后才发起。
- 网页翻译：排除插件字幕框、隐藏文本、可编辑草稿和代码公式；切换原文不覆盖网站后来更新的文字；导出排除已失效节点。
- 停止采集：离屏消息断开时关闭采集文档，释放采集状态；保存失败与字幕显示失败分别提示。

## 加载本地修复版

扩展目录：

`C:\Users\63923\Documents\ChatGPT\翻译开发项目\apps\browser-extension`

1. 先停止正在运行的实时翻译。
   此次修改包含后端处理逻辑，还需重启本地翻译服务，使新逻辑生效；不需要重新下载模型。
2. 在 Chrome 或 Edge 的扩展管理页找到 Quant Scholar Translator，点击重新加载。
3. **刷新课程网页**，关闭并重开学习侧栏。只重新加载扩展不会替换网页里已有的旧脚本。
4. 点击工具栏／扩展列表中的 Quant Scholar 图标显示右下角悬浮按钮，再点击悬浮按钮打开菜单并开始实时翻译；再次点击扩展图标会隐藏悬浮按钮和菜单。无原字幕时，浏览器需要这次点击来授权标签页音频。

此轮没有修改后端模型资源，不需要重新下载模型。上方“翻译引擎”用于实时字幕；网页／PDF 的服务配置在“网页与 PDF 设置”中管理。

## 验证与限制

- 当前累计 220 项扩展测试、98 项 Python 后端测试通过。术语库已升级为 V11 保留式迁移，工具栏控制悬浮入口显示/隐藏继续纳入回归。
- 新增等待修复验证：慢模型多次查询只派发一次、无播放器消息仍推进后续批次、后台重建复用任务编号、第三次尝试仍可查询、网络失败保留断点、旧服务明确报错、10 分钟等待上限、接口同号不同内容拒绝、容量限制及网页来源检查。
- 新增验证：CQF 当前播放器提取、DRM 视频的独立明文字幕路径、逐条 ID 回填、漏项与重复编号拒绝、暂停/倍速/拖动事件、字幕空档及重叠、停止后的迟到回复隔离、完整 WAV 解码及末尾保留、PDF 直接专业引擎约束。
- 两套隔离浏览器界面检查通过：菜单控件、提前/实时独立启动参数、准备进度、双语 MD 下载、PDF 预览及 SRT 原始结束时间。PDF 已逐页检查 4 页样例，18 条原译文和末条时间戳均保留。该界面检查本身使用模拟返回；后续另行完成的 140 条真实 Codex 专业翻译评测见 `docs/PROFESSIONAL_TRANSLATION_EVALUATION.md`。
- 新增验证：重复语句与低音量内容不被规则跳过、未翻译尾句收尾、失败重试、断连缓冲、原文与迟到终稿对应、笔记保存与重新读取、超过 100 条保留、并发保存与存储失败。
- 本地界面测试覆盖菜单按钮、网页翻译与切换、取消与错误恢复、Markdown/JSON/SRT 导出、字幕 DOM 恢复、字号位置、双语对应、重复时间戳及避免二次调用。
- 页面测试使用隔离的本地网页和模拟模型返回，未消耗模型额度。**尚未在登录后的 CQF 课程页面完成真实音频＋模型＋字幕显示的端到端验证。**
- 浏览器控制仍受当前连接故障限制。跨域 iframe 全屏、DRM 音频、所有网站和播放器兼容性不能据此宣称全部已验证。
- 网页扩展不支持 Windows 本地播放器；桌面端开发已按要求撤回。
