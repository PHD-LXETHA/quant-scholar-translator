# Quant Scholar Translator 0.8.1

这是扫描型 PDF 本地 OCR 的稳定性修复版。

- PDF 页面改用自适应 JPEG 编码，显著降低彩色扫描页的 OCR 请求体积。
- 在客户端限制 OCR 页面载荷，避免服务端因请求过大返回 HTTP 422。
- 将裸露的 422 错误改为可操作的中文提示。
- 保留本地 OCR 后直接交给 Codex/Kimi 专业精译的流程。

升级后请在扩展管理页重新加载扩展，并重新打开 PDF 阅读器标签页。
